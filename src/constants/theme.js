import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    fonts: {
        body: 'Montserrat',
        heading: 'Limelight'
    },
    colors: {
        custom: {
            blue: '#004151'
        }
    }
})



export default theme